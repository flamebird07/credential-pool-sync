Step 3: Rewrite switch_next.py to directly compare api_key instead of using hermes auth remove.

## Problem
switch_next.py currently uses `hermes auth remove` to delete credentials. This doesn't work when:
1. The current model's api_key is NOT in the credential pool
2. There's only 1 credential per provider

## Fix
Rewrite switch_next.py to:
1. Read current model config from ~/AppData/Local/hermes/config.yaml via get_current_model_config()
2. Sync Feishu records (with --skip-health-rotate)
3. Compare current api_key against records' api_key
4. If current key NOT in pool → pick first valid record
5. If current key in pool with multiple → pick next one (circular)
6. If current key in pool with only 1 → report "无可用切换"
7. Call update_runtime_main_model() directly (already exists in the file)

Remove: auth_list(), remove_cred() functions and all hermes auth remove logic.
Keep: run_sync(), update_runtime_main_model() as they are.

The file currently has auth_list(), remove_cred(), run_sync(), update_runtime_main_model() functions.
Replace main() and add get_current_model_config(). Remove auth_list() and remove_cred().
